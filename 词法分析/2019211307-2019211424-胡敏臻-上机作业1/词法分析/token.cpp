#include "token.h"

//token::token(tokenType type, int symbolPos, int line, int row) {
//	this->type = type;
//	this->symbolPos = symbolPos;
//	this->line1 = line;
//	this->row1 = row;
//	std::cout << (int)type << symbolPos << line << row;
//}
